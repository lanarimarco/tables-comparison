# AS400 / ges_mu274
SOURCE1_NAME=AS400 / ges_mu274
SOURCE1_JDBC_URL=jdbc:as400://10.250.0.11;libraries=X274DATSUP,X274DATGRU,SMEUP_DEM;naming=system
SOURCE1_USERNAME=
SOURCE1_PASSWORD=
SOURCE1_DRIVER_CLASS=com.ibm.as400.access.AS400JDBCDriver

# PG / kokoslab
SOURCE2_NAME=PG / kokoslab
SOURCE2_JDBC_URL=jdbc:postgresql://10.250.61.13:5432/postgres?currentSchema="X274DATSUP","X274DATGRU","SMEUP_DEM"
SOURCE2_USERNAME=
SOURCE2_PASSWORD=
SOURCE2_DRIVER_CLASS=org.postgresql.Driver

# Table names (comma-separated)
TABLE_NAMES=AUTOAP0F,B£G15M0F,B£MEDE0F,B£_0630F,BRCOMM0F,BRENTI0F,BRESCO0F,C5ADFF0F,C5MOAN0F,C5RATE0F,C5RITE0F,C5RREG0F,C5SE210F,CQRDIN0F,C£CONR0F,C£_1020F,D5COSO0F,EDRECI0F,MEDAV00F,MULANGTF,TABDS00F,TABEL00F,TABELG0F,TABELV0F,TABELX0F,VERAPG0F

# Table schemas (comma-separated)
TABLE_SCHEMAS=X274DATSUP,X274DATGRU,SMEUP_DEM

# Maximum rows to scan per table. Comparison stops at this limit and a TRUNCATED warning is added.
# Omit or set to 0 for no limit (all rows).
# COMPARE_MAX_ROWS=10000

# Number of tables compared in parallel. Defaults to 5.
# Each thread consumes one connection per datasource, so the connection pool is sized to match.
# COMPARE_THREAD_POOL_SIZE=5

# JDBC query timeout in seconds for COUNT(*) and row-scan statements.
# Omit or set to 0 for no timeout.
# COMPARE_QUERY_TIMEOUT_SECONDS=300
